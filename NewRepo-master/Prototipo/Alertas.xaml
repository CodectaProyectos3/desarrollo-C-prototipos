<Window x:Class="Prototipo.Alertas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Estado General del Sistema"
        Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2f">

    <Border CornerRadius="10" BorderThickness="1" Margin="10">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="DarkRed" Offset="0"/>
                <GradientStop Color="Blue" Offset="0.5"/>
                <GradientStop Color="Purple" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: Título + botones -->
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,10">
                <TextBlock Text="🔔  Alertas Activas"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="5"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="▁" Width="30" Height="30"
                            Background="Transparent" BorderBrush="Transparent"
                            Foreground="White" FontSize="14"
                            Click="BtnPeque_Click" Cursor="Hand"/>
                    <Button Content="✖" Width="30" Height="30"
                            Background="Transparent" BorderBrush="Transparent"
                            Foreground="White" FontSize="14"
                            Click="BtnCerrar_click" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>

            <!-- Lista de Alertas -->
            <ScrollViewer Grid.Row="1">
                <StackPanel x:Name="AlertContainer" Margin="5">
                    <!-- Ejemplo de alerta -->
                    <Border Background="#2a2a3d" Padding="10" CornerRadius="6" Margin="0 5 0 5">
                        <DockPanel>
                            <TextBlock Text="⚠" FontSize="20" Foreground="Orange" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="Temperatura crítica en CPU" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="El sistema ha detectado una temperatura mayor a 85°C." Foreground="Gray" FontSize="12"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <!-- Puedes repetir más bloques similares o agregarlos dinámicamente -->
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <TextBlock Grid.Row="2"
                       Text="Estado general del sistema - Última actualización: tiempo real"
                       Foreground="Gray"
                       FontSize="11"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </Grid>
    </Border>
</Window>

